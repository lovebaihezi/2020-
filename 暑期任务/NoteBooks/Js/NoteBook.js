function FlexBox(number, status, node) {
    this.Number = number;
    this.Status = status;
    this.Node = Node;
}
FlexBox.prototype.SetNumber = function(PremierOfThisElement) {
    if (PremierOfThisElement == NULL) {
        this.Number = 1;
    } else {
        this.Number = PremierOfThisElement.Number + 1;
    }
}

FlexBox.prototype.SwitchToTextArea = function() {
    FlexBox.
}

FlexBox.prototype.NodeList = NodeListGet();

FlexBox.prototype.NodeListGet = function() {

}

window.onload = function() {
    var Number = document.getElementsByClassName("flex").length;
}